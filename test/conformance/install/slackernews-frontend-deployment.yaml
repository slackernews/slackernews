apiVersion: apps/v1
kind: Deployment
metadata:
  name: slackernews-frontend
  namespace: slackernews
spec:
  selector:
    matchLabels:
      app: slackernews-frontend
  template:
    metadata:
      labels:
        app: slackernews-frontend
